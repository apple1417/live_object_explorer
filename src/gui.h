#ifndef GUI_H
#define GUI_H

namespace live_object_explorer::gui {

/**
 * @brief Opens the gui, if it isn't already.
 */
void show(void);

/**
 * @brief Renders the gui.
 */
void render(void);

}  // namespace live_object_explorer::gui

#endif /* GUI_H */
